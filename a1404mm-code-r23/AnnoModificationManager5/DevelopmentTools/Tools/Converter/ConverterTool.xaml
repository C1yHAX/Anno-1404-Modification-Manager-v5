<UserControl x:Class="DevelopmentTools.Tools.Converter.ConverterTool"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="480" d:DesignWidth="640">
    <Grid Background="{DynamicResource Brush_UI_White}">
        <Border Height="44" VerticalAlignment="Top" Background="#FF132238">
            <TextBlock Text="Mod-Paket konvertieren" Foreground="#FFF59E0B" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" Margin="14,0,0,0"/>
        </Border>
        <TabControl Margin="8,52,8,42" Name="tabControl">
        	<TabItem Header="Settings" IsEnabled="False">
        		<Grid Background="{DynamicResource Brush_UI_White}">
        			<Grid Margin="8,8,8,0" VerticalAlignment="Top">
        				<Grid HorizontalAlignment="Left" Width="100">
        					<TextBlock Text="Old Project" d:LayoutOverrides="Width, Height" VerticalAlignment="Center" HorizontalAlignment="Left"/>
        				</Grid>
        				<TextBox TextWrapping="NoWrap" d:LayoutOverrides="Height" Margin="100,0,29,0" IsEnabled="False" x:Name="Field_OldProject" />
        				<Button Content="..." HorizontalAlignment="Right" Width="25" d:LayoutOverrides="Height" Margin="0" Background="{DynamicResource Brush_UI_White}" x:Name="Field_OldProject_Open" Click="Field_OldProject_Open_Click" />
        			</Grid>
        			<Grid Margin="8,33.96,8,0" VerticalAlignment="Top">
        				<Grid HorizontalAlignment="Left" Width="100">
        					<TextBlock Text="New Project" d:LayoutOverrides="Width, Height" VerticalAlignment="Center" HorizontalAlignment="Left"/>
        				</Grid>
        				<TextBox TextWrapping="NoWrap" d:LayoutOverrides="Height" Margin="100,0,29,0" IsEnabled="False" x:Name="Field_NewProject" />
        				<Button Content="..." HorizontalAlignment="Right" Width="25" d:LayoutOverrides="Height" Margin="0" Background="{DynamicResource Brush_UI_White}" x:Name="Field_NewProject_Open" Click="Field_NewProject_Open_Click" />
        			</Grid>
        		</Grid>
        	</TabItem>
        	<TabItem Header="Status" IsEnabled="False">
        		<Grid Background="{DynamicResource Brush_UI_White}">
        			<TextBox x:Name="Field_Status" d:LayoutOverrides="Width, Height" IsReadOnly="True" />
        		</Grid>
        	</TabItem>
			<TabItem Header="Result" IsEnabled="False">
        		<Grid Background="{DynamicResource Brush_UI_White}"/>
        	</TabItem>
        </TabControl>
        <DockPanel Height="30" Margin="8,0,8,8" VerticalAlignment="Bottom" LastChildFill="False">
        	<Button x:Name="button_Patch" Content="Convert" Width="100" DockPanel.Dock="Right" Background="{DynamicResource Brush_UI_White}" Margin="0" Click="button_Patch_Click" />
            <Button Click="button_Cancel_Click" x:Name="button_Cancel" Content="Cancel" Width="100" DockPanel.Dock="Right" Background="{DynamicResource Brush_UI_White}" Margin="0,0,5,0" />
        </DockPanel>
    </Grid>
</UserControl>
